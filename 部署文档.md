# 部署文档（详细）

适用对象：需要把 **Printer-Monitor** 部署到 Windows 或 Linux 的运维 / 管理员。
配套文件：`server.js`、`printer-monitor.html`、`node_modules/`、`install.sh`（Linux）、`start.bat`（Windows）、`printers.json`。

> **📥 获取安装包**：到本仓库 **Releases** 页面下载对应系统的 zip —— 🪟 `printer-monitor-windows.zip`（Windows）/ 🐧 `printer-monitor-linux.zip`（Linux）。解压即用，无需从源码构建。

---

## 一、环境要求

| 项目 | 要求 |
|------|------|
| 运行环境 | Node.js **18+**（Windows 便携版已内置 `node.exe`，无需安装） |
| 依赖 | `net-snmp`（已随包放在 `node_modules/`，离线可用） |
| 网络 | 运行监控的机器必须能 **ping 通打印机所在网段**，且打印机已开启 **SNMP v2c**（团体名默认 `public`）。打印机可在同一网段，也可分散在多个网段 / 区域，平台逐台按 IP 监控、不区分拓扑 |
| 端口 | Web 面板默认 **8899**（可改）；需放行防火墙 |

> 纯 Node.js 实现，无数据库、无消息队列、无第三方服务，单目录即可运行。

---

## 二、Windows 部署（便携版，给同事直接运行）

### 方式 A：从 GitHub Releases 下载（推荐，最省事）

1. 打开本仓库 **Releases** 页面，下载 `printer-monitor-windows.zip`，解压到任意目录（桌面 / D 盘等**普通用户目录**即可；**不要**放 `C:\Program Files`，否则写配置需管理员权限）。
2. **双击 `start.bat`**。
3. 自动弹出黑色服务窗口（勿关闭），浏览器自动打开 `http://localhost:8899`。
4. 在面板「扫描网段添加打印机」或「添加打印机」填入打印机 IP。
5. 关闭黑色窗口 = 停止监控。

### 系统密码 / 登录（Windows，重点）

- **默认不启用登录、没有密码**：便携版 `printers.json` 里没有 `auth` 段时，打开面板即为管理员，添加 / 删除 / 扫描等按钮全部直接可用。适合本机或内网信任环境，**无需任何密码**。
- **需要开启登录时**（这台 Windows 会被别人通过浏览器访问、或想保护写操作），在本目录命令行执行：
  ```
  set-password.bat --rotate              # 自动生成 18 位强密码并启用登录
  set-password.bat 你的新密码             # 指定密码（>=12 位，含大小写字母和数字）
  ```
  启用后：用户名 `admin`；登录入口 = 点面板左上角打印机 logo（平时隐藏）；**明文密码保存在本目录 `.admin-password.txt`**（以 `.` 开头的隐藏文件）；改完需**关闭黑色服务窗口，重新双击 `start.bat`** 生效。
- 想关掉登录恢复“免密直通”？编辑 `printers.json` 删除整个 `"auth"` 段，保存重启即可。
- 说明：Windows 与 Linux 共用同一套登录机制（scrypt 哈希），只是 Windows 用 `set-password.bat`、Linux 用 `change-password.sh`。

### 改端口（Windows）

- 编辑 `printers.json` 的 `"port": 8899` 为你想要的端口，保存后重启 `start.bat`；或
- 右键 `start.bat` → 编辑，把最后一行改为：
  ```
  start "" "node.exe" server.js --port 9260
  ```

### Windows 防火墙放行（他人需访问面板时）

若其他电脑要访问该面板，在运行监控的 Windows 上：
```
netsh advfirewall firewall add rule name="PrinterMonitor" dir=in action=allow protocol=TCP localport=8899
```

### 开机自启（可选）

- **计划任务法**：任务计划程序 → 创建任务 → 触发器“计算机启动时” → 操作“启动程序”指向 `start.bat`，勾选“不管用户是否登录都要运行”。
- 或用 `nssm` 把 `node.exe server.js` 注册为 Windows 服务（更稳）。

### 升级（Windows）

用新版本覆盖 `server.js`、`printer-monitor.html`、`node_modules/` 三个文件，重启 `start.bat` 即可。`printers.json` 保留不动。

---

## 三、Linux 部署（systemd，推荐作为对外服务）

### 一键安装

```bash
# 1) 获取安装包（二选一）：
#    a) 从 Releases 下载 printer-monitor-linux.zip，解压；或
#    b) git clone https://github.com/Hadesls/Printer-Monitor.git
# 2) 进入该目录，root 执行安装脚本：

sudo bash install.sh            # 默认端口 8899
sudo PORT=9260 bash install.sh  # 自定义端口
```

脚本会自动完成：
1. 安装 Node.js（若未装，用 `dnf`；AlmaLinux / RHEL / CentOS 通用；Debian 系请先 `apt install nodejs`）。
2. 把 `server.js` / `printer-monitor.html` 部署到 `/opt/printer-monitor`。
3. 安装 SNMP 依赖（有网 → `npm install net-snmp`；无网 → 用随包 `node_modules/` 离线副本）。
4. 首次生成管理员账户（随机强密码，写入 `/opt/printer-monitor/.admin-password.txt`，权限 600）。
5. 创建专用运行用户 `printermon`，配置权限。
6. 注册 `systemd` 服务 `printer-monitor`（开机自启、崩溃 5 秒自拉起）。
7. 放行防火墙端口（firewalld 运行时自动加；未运行则提示手动命令）。
8. 健康检查，输出面板地址、用户名与初始密码。

安装成功后输出示例：
```
面板地址:  http://<服务器IP>:8899/login
用户名:    admin
初始密码:  <随机强密码>
密码文件:  /opt/printer-monitor/.admin-password.txt
```

### 访问面板

- 同机：`http://localhost:8899`
- 局域网其他电脑：`http://<服务器IP>:8899`

### 服务管理

```bash
systemctl status printer-monitor      # 状态
systemctl restart printer-monitor     # 重启
systemctl stop printer-monitor        # 停止
journalctl -u printer-monitor -f       # 实时日志
```

### 防火墙（手动，若 install.sh 提示未放行）

```bash
firewall-cmd --permanent --add-port=8899/tcp
firewall-cmd --reload
```

### 升级（Linux）

把新的 `server.js` / `printer-monitor.html` 放到原安装目录，**重新执行**：
```bash
sudo PORT=8899 bash install.sh     # 端口以本次为准；printers.json 与账户自动保留
```
等价于“覆盖程序文件 + 重启服务”。

### 改密码

```bash
sudo bash change-password.sh                 # 交互式，需满足强密码策略(≥12位含大小写与数字)
sudo bash change-password.sh --rotate        # 自动生成 18 位强密码并立即生效（推荐定期轮换）
```
改密后所有在线会话注销，服务自动重启。

### 卸载

```bash
systemctl stop printer-monitor
systemctl disable printer-monitor
rm -f /etc/systemd/system/printer-monitor.service
systemctl daemon-reload
rm -rf /opt/printer-monitor
userdel printermon 2>/dev/null
```

---

## 四、配置说明（printers.json）

首次启动若无 `printers.json`，会自动生成一份空配置。常用字段：

| 字段 | 说明 |
|------|------|
| `community` | SNMP 团体名（v2c），默认 `public` |
| `pollIntervalSec` | 轮询间隔秒数，默认 10 |
| `port` | Web 面板端口，默认 8899 |
| `printers[].name` | 显示名 |
| `printers[].ip` | 打印机 IP |
| `printers[].location` | 位置 / 区域备注：**纯自由文本、只作展示**，与 IP 网段无关，系统不按 IP 自动归类。叫法随意：按部门（财务室 / 仓库）、楼层（一楼 / 二楼）、网络性质（内网区 / 外网区）均可。文档 / 示例里出现的「办公区 / 实验区」只是作者给自家两个网段起的名字（`10.x` 段叫「办公区」、`192.168.x` 段叫「实验区」，可类比外网区 / 内网区），并无特殊含义——你可换成任何自己看得懂的名字，也可全部相同或留空 |
| `printers[].community` | 单台覆盖团体名（可选） |
| `printers[].demo` | `true` = 模拟设备（无需真实硬件即可演示） |
| `printers[].suppressed` | `true` = 永久屏蔽该设备告警 |
| `auth` | 登录认证段（由安装脚本生成，详见下） |

### 启用登录认证（对外部署建议）

**默认（未配置 `auth`）= 无需登录、打开即管理员**。需要"公开只读 + 写操作需登录"时，在 `printers.json` 增加 `auth` 段（也可直接用下方工具一键生成）：
```json
"auth": {
  "enabled": true,
  "sessionTimeoutMin": 480,
  "users": [ { "username": "admin", "salt": "<32hex>", "hash": "<64hex>" } ]
}
```
> **推荐直接用现成工具生成 `auth` 段**（不用手写盐/哈希）：Linux 用 `sudo bash change-password.sh`；Windows 用同目录 `set-password.bat --rotate`。
密码以 **scrypt 哈希**形式存于 `printers.json`（**不存明文**）；管理员明文另存 `.admin-password.txt`（权限 600）供查阅。启用后：
- 面板为**公开只读**，任何人可看状态；
- 点击标题 logo / 执行写操作弹登录框，登录后显示写操作按钮；
- 登录失败 5 次锁定该 IP 15 分钟；会话 Cookie 为 HttpOnly + SameSite=Strict。

---

## 五、网络模型与离线判定（重要）

本平台按 **IP 轮询**：能拉到 SNMP = 在线；超时 = 离线。这与“交换机端口绑定 MAC”的网络的语义天然吻合：

- **IP 不通** → 该交换机端口当前没有合法设备（设备被搬走 / 未接好）= 真离线 ✅
- **IP 能通** → 该端口上当前是有合法 MAC 的设备（你更新了绑定，或搬回原机）= 在线 ✅

因此**无需担心“误把新设备当旧设备”**：设备名 / 位置是 `printers.json` 里固定写死的，按 IP 轮询不会自动变化；若你换了同 IP 的新打印机，状态会显示在线但名字仍是配置里的旧名（如需“设备更换检测”可后续扩展比对 MAC / 序列号）。

---

## 六、排错

| 现象 | 排查 |
|------|------|
| 面板打不开 | 确认服务在跑（`systemctl status` / 看黑色窗口）；确认端口与防火墙；用 `curl http://127.0.0.1:8899/api/health` 验证后端 |
| 全部显示离线 | 监控机能否 `ping` 通打印机 IP？打印机 SNMP 是否开启？团体名是否与 `community` 一致？ |
| HP 机型误报缺纸 | 已知兼容处理：仅容量可信且余量 0 才判缺纸；如仍误报，检查 `prtAlertTable` 是否真有 1006/1005 |
| 写操作提示需登录 | 正常：公开只读，写操作需点标题图标登录 |
| 端口被占用 | 换端口（`--port` 或 `printers.json` 的 `port`），并确认无其它进程占用 |
| Linux 依赖装不上（无外网） | 用随包 `node_modules/` 离线副本；install.sh 会自动回退；或手动 `cp -r node_modules /opt/printer-monitor/` |
| 浏览器显示旧状态 | `Ctrl+F5` 强制刷新（前端已加 `no-store`，一般无需） |

---

## 七、目录清单（交付包应包含）

**Windows 包**：`node.exe`、`server.js`、`printer-monitor.html`、`node_modules/`、`start.bat`、`printers.json`、`package.json`、`README.txt`、`1.ico`、`2.png`
**Linux 包**：`server.js`、`printer-monitor.html`、`node_modules/`、`install.sh`、`change-password.sh`、`printers.json`、`printers.json.example`、`README.md`、`部署文档.md`、`使用帮助.md`、`.gitignore`、`1.ico`、`2.png`
