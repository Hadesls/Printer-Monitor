# 使用帮助（面板使用手册）

本文面向**日常使用监控面板的同事**，讲清楚面板怎么看、怎么加打印机、告警怎么处理。
部署相关请看 `部署文档.md`。

面板地址：`http://<服务器IP>:8899`（Windows 本机为 `http://localhost:8899`）。

---

## 一、面板布局

打开面板后，从上到下依次是：

1. **顶栏**：左侧是打印机 logo 标题（**点这个图标 = 登录入口**，平时隐藏，管理员才用）；右侧显示后端连接状态徽标。
2. **统计卡片**：打印机总数、在线、墨粉不足、离线 / 故障、缺纸告警、卡纸告警、累计用纸（页）。
   - 前 6 张**可点击筛选**；「累计用纸」为纯展示。
3. **筛选条**：`墨粉不足` / `离线` 等快捷标签，与统计卡片联动。
4. **打印机列表**：序号、名称、IP、位置、状态、墨粉余量、操作（详情）。
5. **墨粉余量分布**：图形化展示各机墨粉。
6. **告警通知面板**：按分类（墨粉 / 缺纸 / 卡纸 / 离线）展示当前告警。

---

## 二、查看设备状态

- 列表里每台打印机显示：在线 / 离线（故障）、墨粉余量百分比、位置。
- 点「**详情**」打开弹窗，可见：
  - 墨粉详情（黑白 / 彩色四色条）
  - 纸盒余量（每个纸盒当前余量 / 容量）
  - 设备状态、累计页数
  - 「网络连接」信息（**仅管理员登录后可见**）

---

## 三、添加打印机（管理员）

> 添加 / 删除 / 扫描 / 发送告警等**写操作需先登录**（点顶部 logo 图标，弹极简登录框：用户名 + 密码 + 登录）。

三种方式：

1. **手动添加**：登录后点「＋ 添加打印机」，填名称、IP、位置；可选彩色打印机、初始墨粉（真实设备无需填，SNMP 自动读）。
2. **扫描网段**：点「扫描网段添加打印机」（或「自动发现本机网段」），填子网（如 `192.168.54.0/24` 或 `10.0.8`），系统 ping + TCP 探测 + SNMP 识别，批量入库，自动去重。
3. **批量添加**：`POST /api/printers/batch` 传入 `[{ip, name?, location?}]` 批量写入。

> **「位置 / 区域」怎么填？** 这是**自由文本、纯备注**，跟 IP 网段没有绑定——可以按区域叫（`办公区` / `实验区`，这纯粹是个人命名习惯），也可以按部门（`财务室` / `人事部`）、按楼层（`一楼` / `二楼`）分，甚至可以**所有打印机填同一个名字、或直接留空**。系统只拿它来分区展示、定位告警，不做任何判断；只有一台打印机 / 只有一个网段的话，填你自己的习惯即可。

添加后系统立即开始 SNMP 轮询，状态实时更新。

---

## 四、告警处理

告警分四类：**墨粉不足 / 缺纸 / 卡纸 / 离线（故障）**。每条告警显示“多久前提醒”的计时。

- **「知道了」**：限时静音。点后该条在默认 **1 小时**内不再提示；超时后或设备状态恢复（如换好墨粉 / 卡纸解除）会重新出现。计时同步重置。
- **「屏蔽」**：永久静音该设备 / 该条告警，直到你主动解除。
- **「解除通知屏蔽」**：管理员在「**已屏蔽 / 已静音**」区可见被屏蔽项，点此按钮恢复提醒。
  - 该区**仅管理员可见**；访客（未登录）连“存在屏蔽”都不显示。

> 设计原则：公开只读，告警清理 / 屏蔽都是管理员动作，避免误改。

---

## 五、统计卡片点击筛选（快速定位）

- 点「**在线**」→ 只看在线设备；
- 点「**墨粉不足**」→ 只看墨粉告警设备；
- 点「**离线 / 故障**」「**缺纸告警**」「**卡纸告警**」同理；
- 点「**打印机总数**」→ 清除筛选，回到全部。
- 再点同一张卡片 = 取消该筛选。当前筛选会高亮对应卡片。

适合日常“一眼定位故障机”。

---

## 六、登录与退出

- **登录入口平时隐藏**（安全考虑）。方式：
  - 点顶部**打印机 logo 图标**（标题左侧）；
  - 或在详情弹窗点「发送告警通知」（无会话时自动弹登录框）。
- 登录框极简：仅用户名、密码、居中的「登录」按钮。
- 登录后顶栏右侧显示「👤 用户名 · 退出」，点「退出」注销。
- 访客状态：仅能看状态、点统计卡片筛选、看告警；不能添加 / 删除 / 扫描 / 发送告警。

---

## 七、发送告警通知

在设备详情弹窗点「**发送告警通知**」（需登录），会弹出确认框，提示已将该设备告警通知至运维团队（含设备名 / 位置 / IP）。

> 当前为**前端确认**（手动标记“已通知”）。如需自动推送到邮件 / 钉钉 / 企业微信，需在后端 `server.js` 扩展 webhook / SMTP（见 README 常见问题）。

---

## 八、离线说明

- 打印机 IP 不通（SNMP 轮询超时）即标「离线」。在“交换机端口绑定 MAC”的网络里，IP 不通 = 设备确实不在 / 未接好 = 真离线。
- 设备搬回原端口并联网（MAC 绑定更新）后，≤10 秒自动转在线。
- 若浏览器显示旧状态，按 `Ctrl+F5` 强制刷新。

---

## 九、常见疑问

**Q：为什么我看不到“添加打印机”按钮？**
A：那是写操作，需登录。点顶部 logo 图标登录后即出现。

**Q：我点了“知道了”，怎么又出现了？**
A：“知道了”只是限时静音（默认 1 小时），到点或状态未恢复会重新提示；若想彻底不提示用「屏蔽」。

**Q：墨粉明明换了还报警？**
A：状态恢复（SNMP 读到新余量）后告警会自动消失；若仍报，可能 SNMP 未刷新，点一下刷新或等下一个轮询周期。

**Q：我们打印机只有一个网段（甚至只有几台），能用吗？**
A：能。平台对网段数量没有要求——每台打印机只要能 ping 通且开启 SNMP，填个 IP 就能加入；「位置」只是自由备注，与网段无关（按区域 / 部门 / 楼层自己命名都行，也可全填一样或留空）。打印机分散在多个网段也只是多填几台 IP，扫描时对每个网段各填一次即可。例子里出现不同网段只是为了演示「跨网段也支持」。

**Q：累计用纸怎么算的？**
A：全部打印机 `pages`（累计页数）之和，统计卡显示「累计用纸（页）」。
